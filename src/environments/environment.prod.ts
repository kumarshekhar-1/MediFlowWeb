export const environment = {
  production: true,
  // Replace with your AWS Backend URL, e.g.:
  // 'https://api.yourdomain.com' or 'http://<YOUR_AWS_EC2_IP>:8080' or '/api' (if using Nginx reverse proxy)
  apiUrl: 'http://localhost:8080'
};
